// Injected at build time from package.json. Keeps version in one place.
// eslint-disable-next-line @typescript-eslint/no-require-imports
export const VERSION: string = require("../package.json").version;
