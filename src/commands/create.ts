// Placeholder for `bulma create <site-name>`.
// Real implementation (GitHub repo creation, shell-template clone,
// Vercel project setup, workflow generation) lands in subsequent PRs.
export function createSite(_siteName: string): never {
  throw new Error("bulma create is not implemented yet");
}
